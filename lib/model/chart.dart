class toChart {
  final int? id, quantity, price, id_user;

  String? name, desc, image;

  toChart(
      {this.id,
      this.name,
      this.quantity,
      this.image,
      this.desc,
      this.price,
      this.id_user});
}
